parameters:
    git_dir: .
    bin_dir: ./vendor/bin
    tasks:
        phpcs:
            standard: PSR2
            show_warnings: false
            tab_width: ~
            ignore_patterns: ['tests/', 'database/']
            sniffs: []
        phpunit:
            config_file: ~
